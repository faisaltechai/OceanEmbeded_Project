fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
numpy>=1.26
pandas>=2.2
PyYAML>=6.0
python-multipart>=0.0.9

# Live satellite gateway layer
httpx>=0.27
apscheduler>=3.10

# Caching
redis>=5.0

# Database / geospatial (Postgres + PostGIS app-state layer)
sqlalchemy>=2.0
asyncpg>=0.29
alembic>=1.13
geoalchemy2>=0.15

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# Report generation
reportlab>=4.1

# Optional, heavier deps needed only by specific live sources -- install
# these when you actually configure that provider's credentials:
#   copernicusmarine>=1.3   # Copernicus Marine Service subsetting
#   cdsapi>=0.7             # ERA5 via Copernicus CDS
#   xarray>=2024.2          # NetCDF handling for both of the above
#   netCDF4>=1.6
